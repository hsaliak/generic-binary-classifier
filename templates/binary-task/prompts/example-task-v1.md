# Example task prompt

Generate JSONL candidate records for `example-task`. Each record must include the configured input fields, one of the labels `allow` or `block`, a family, batch ID, source provenance, and rationale. Define boundary cases in this prompt before use. Do not include personal data, secrets, or duplicate inputs. Candidate output is unreviewed material only.
